#import * from "Schema.graphql";

input ImagesInput {
    state: String!
    page: Int!
    classEmoji: String
}

input ImageInput {
    id: String!
}

input UploadInput {
    image: Upload!
    class: String!
}

input LikeInput {
    id: String!
}