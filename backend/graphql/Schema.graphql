#import * from "Types.graphql";
#import * from "Inputs.graphql";

scalar Upload
type Query {
    images (input: ImagesInput!): [Image]!
    image (input: ImagesInput!): Image!
    iclasses: [ImageClass]!
    like (input: LikeInput): Like!
    likes: [Like]!
}

type Mutation {
    uploadImage (input: UploadInput!): Boolean
}